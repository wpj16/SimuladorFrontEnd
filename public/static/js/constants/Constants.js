export const CSRF_TOKEN = $('meta[name="csrf-token"]').attr('content');
export const URL = window.location.protocol + '//' + window.location.hostname;
export const URI = window.location.pathname;
export const SEARCH = window.location.search;
export const DOMAIN = window.location.hostname;
export const LOADING = URL + '/static/img/sistema/TradeTechnology/loading.png';
